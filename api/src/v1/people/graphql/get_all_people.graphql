# Get all people with basic info (for list views)
query GetAllPeople($offset: Int, $limit: Int) {
    people(offset: $offset, limit: $limit) {
        id
        name
        first_name
        surname
        academic_degree
        faculty {
            id
        }
    }
}
