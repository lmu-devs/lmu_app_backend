query clubs($languageCode: String!) {
    student_clubs(sort: ["translations.title"], filter: { is_shown: { _eq: true } }) {
        id
        univerities {
            id
        }
        type
        logo {
            filename_download
        }
        url
        email
        instagram_url
        linkedin_url
        category
        translations(filter: { languages_code: { code: { _eq: $languageCode } } }) {
            title
            description
            content
        }
    }

}